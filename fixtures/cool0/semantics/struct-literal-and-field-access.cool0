// expect: f() == 34
struct Point { x: i32, y: i32 }
fn move_by(p: &mut Point, dx: i32, dy: i32) { p.^.x += dx; p.^.y += dy; }
fn f() -> i32 { let p: Point = Point{ x: 3, y: 4 }; return p.x * 10 + p.y; }
